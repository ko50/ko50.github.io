import 'dart:io';

// import 'package:dotenv/dotenv.dart';

const double widthBreakpoint = 630;

const int transitionDefaultDuration = 500;

const Duration initialLoadDuration = Duration(milliseconds: 900);

const Duration footerAnimateDuration = Duration(milliseconds: 700);

const Duration footerCloseDelay = Duration(milliseconds: 900);

const Duration drawerAnimateDuration = Duration(milliseconds: 250);

final String apiUriRoot = 'https://api.ko50.dev/api/v1';
